resources:
  pipelines:
    test_pipeline:
      name: anisha_pipeline
      serverless: true
      catalog: mc_sme
      target: proj1_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb
      configuration:
        bundle.sourcePath: ${workspace.file_path}/src

  sql_objects:
      test_table:
        type: query
        file: ../sql/create_test_table.sql