# This is a Databricks asset bundle definition for proj1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: proj1

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-c74c987d-541c.cloud.databricks.com

  # prod:
  #   mode: production
  #   workspace:
  #     host: https://dbc-c74c987d-541c.cloud.databricks.com
  #     # We explicitly specify /Workspace/Users/crmpravin@gmail.com to make sure we only have a single copy.
  #     root_path: /Workspace/Users/crmpravin@gmail.com/.bundle/${bundle.name}/${bundle.target}
  #   permissions:
  #     - user_name: crmpravin@gmail.com
  #       level: CAN_MANAGE
  #   run_as:
  #     user_name: crmpravin@gmail.com
