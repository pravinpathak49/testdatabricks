bundle:
  name: proj1

include:
  - resources/*.yml

variables:
  catalog:
    description: "The catalog to use"
    type: string
  schema:
    description: "The schema (database) to use"
    type: string
  

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-c74c987d-541c.cloud.databricks.com
    variables:
      catalog: workspace
      schema: ap
  uat:
    mode: development
    workspace:
      host: https://dbc-e4d34db1-dacb.cloud.databricks.com
    variables:
      catalog: mc_sme
      schema: ap
